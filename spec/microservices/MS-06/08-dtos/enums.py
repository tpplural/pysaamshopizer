"""Enum classes (source: MS-06/04-api-contract.yaml components/schemas).

The MS-06 cart contract defines no `enum` schemas — all enumerable concerns
(totals codes, error classes) are free-form strings or delegated to MS-09.
This module is intentionally empty to satisfy the standard DTO package layout.
"""

from __future__ import annotations
